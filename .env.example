# Firebase – copy to .env and fill with your project values from Firebase Console
# https://console.firebase.google.com/ → Project settings → Your apps

PUBLIC_FIREBASE_API_KEY=your-api-key
PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
PUBLIC_FIREBASE_PROJECT_ID=your-project-id
PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
PUBLIC_FIREBASE_APP_ID=1:123456789:web:abc123

# Supabase – public client (used by frontend and server endpoints)
# Dashboard → Project Settings → API
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-jwt

# Google Maps – needed for route display on the transfer results page
# Enable: Maps JavaScript API, Directions API, Geocoding API
PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Stripe – needed for payment processing
PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key

# Stripe — server-side capture (added 2026-05-04 for Checkout flow)
# Get test keys from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key
# Get from `stripe listen --forward-to localhost:4321/api/stripe/webhook`
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-signing-secret

# Supabase service role — needed by Stripe webhook handler to bypass RLS
# Dashboard → Project Settings → API → service_role (NEVER expose to browser)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-jwt

# Resend — transactional email (booking confirmations, reviews, etc.)
# Dashboard → API Keys. Domain (EMAIL_FROM) must be verified in Resend.
RESEND_API_KEY=re_your-resend-api-key
EMAIL_FROM=info@opawey.com
ADMIN_EMAIL=info@opawey.com

# Google reviews (added 2026-09-17) — server-side only, never PUBLIC_
# A key with "Places API" (and, if the Cloud project still has it, the legacy
# Places API) enabled. Do NOT reuse PUBLIC_GOOGLE_MAPS_API_KEY: that one is
# restricted to browsers.
GOOGLE_PLACES_API_KEY=your-server-side-places-api-key
# The business's Place ID (ChIJ…), from https://developers.google.com/maps/documentation/places/web-service/place-id
GOOGLE_PLACE_ID=ChIJ...
# Random string; Vercel sends it as `Authorization: Bearer <CRON_SECRET>` on
# cron calls to /api/admin/sync-google-reviews. `openssl rand -hex 32`
CRON_SECRET=change-me
