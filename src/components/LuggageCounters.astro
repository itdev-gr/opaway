---
interface Props {
	/** Unique id for the root wrapper so multiple instances on one page don't collide. */
	rootId: string;
	/** Optional label above the two cards. Default "Luggage". */
	label?: string;
}
const { rootId, label = 'Luggage' } = Astro.props;
---

<div id={rootId} class="flex flex-col gap-2.5">
	<span class="block text-sm font-medium text-neutral-700" data-i18n-el="Αποσκευές">{label}</span>

	<div class="grid grid-cols-2 gap-3">

		<!-- Small (carry-on) -->
		<div data-luggage="small" class="flex items-center justify-between rounded-xl border border-neutral-200 bg-white px-3 py-2.5">
			<div class="flex flex-col leading-tight">
				<span class="text-sm font-semibold text-neutral-800" data-i18n-el="Μικρές">Small</span>
				<span class="text-[11px] text-neutral-400" data-i18n-el="Χειραποσκευή">Carry-on</span>
			</div>
			<div class="flex items-center gap-2">
				<button type="button" data-action="minus" aria-label="Decrease small luggage" data-i18n-aria-el="Μείωση μικρών αποσκευών"
					class="w-7 h-7 rounded-lg border border-neutral-200 text-neutral-700 hover:bg-neutral-50 disabled:opacity-40 disabled:cursor-not-allowed transition-colors flex items-center justify-center">
					<svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M20 12H4" /></svg>
				</button>
				<span data-value class="w-6 text-center text-sm font-semibold tabular-nums">0</span>
				<button type="button" data-action="plus" aria-label="Increase small luggage" data-i18n-aria-el="Αύξηση μικρών αποσκευών"
					class="w-7 h-7 rounded-lg border border-neutral-200 text-neutral-700 hover:bg-neutral-50 disabled:opacity-40 disabled:cursor-not-allowed transition-colors flex items-center justify-center">
					<svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" /></svg>
				</button>
			</div>
		</div>

		<!-- Big (checked) -->
		<div data-luggage="big" class="flex items-center justify-between rounded-xl border border-neutral-200 bg-white px-3 py-2.5">
			<div class="flex flex-col leading-tight">
				<span class="text-sm font-semibold text-neutral-800" data-i18n-el="Μεγάλες">Big</span>
				<span class="text-[11px] text-neutral-400" data-i18n-el="Παραδοτέα">Checked</span>
			</div>
			<div class="flex items-center gap-2">
				<button type="button" data-action="minus" aria-label="Decrease big luggage" data-i18n-aria-el="Μείωση μεγάλων αποσκευών"
					class="w-7 h-7 rounded-lg border border-neutral-200 text-neutral-700 hover:bg-neutral-50 disabled:opacity-40 disabled:cursor-not-allowed transition-colors flex items-center justify-center">
					<svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M20 12H4" /></svg>
				</button>
				<span data-value class="w-6 text-center text-sm font-semibold tabular-nums">0</span>
				<button type="button" data-action="plus" aria-label="Increase big luggage" data-i18n-aria-el="Αύξηση μεγάλων αποσκευών"
					class="w-7 h-7 rounded-lg border border-neutral-200 text-neutral-700 hover:bg-neutral-50 disabled:opacity-40 disabled:cursor-not-allowed transition-colors flex items-center justify-center">
					<svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2.5"><path stroke-linecap="round" stroke-linejoin="round" d="M12 4v16m8-8H4" /></svg>
				</button>
			</div>
		</div>

	</div>
</div>
